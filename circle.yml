machine:
  python:
    version: 2.7.9
  environment:
    SKIP_CLEANUP: true
    # comma separated list
    CIRCLECI_FLAVORS: docker

dependencies:
  override:
    - bundle install

test:
  override:
    - ./tests/circleci.sh:
        parallel: true
